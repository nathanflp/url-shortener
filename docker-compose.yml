services:
  db:
    image: postgres:13-alpine
    container_name: url_shortener-db
    restart: always
    environment:
      POSTGRES_DB: url_shortener-db
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"